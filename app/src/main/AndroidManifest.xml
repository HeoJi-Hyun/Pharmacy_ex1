<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<!--    <uses-permission android:name="android.permission.CAMERA"/>-->
<!--    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>-->
<!--    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>-->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Join" android:label="회원가입"/>
        <activity android:name=".Home" android:label="지도"/>
        <activity android:name=".Alarm" android:label="알람기능"/>
        <activity android:name=".Prov1" android:label="타이레놀"/>
        <activity android:name=".Chek" android:label="체크리스트"/>
        <activity android:name=".Prov2" android:label="까스활명수"/>
        <activity android:name=".Medicine" android:label="약정보제공"/>

        <receiver android:name=".Alarm_Reciver" />

        <service
            android:name=".RingtonePlayingService"
            android:enabled="true"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC-F45JLzJoSY5rxgIfBDOB72PYtCNHIUg"/>
    </application>

</manifest>